version: "3.9"
services:
  wol:
    build: wol/.
    ports:
      - 8080:8080
    environment:
      - IP=${IP}
      - MAC=${MAC}
  tailscale:
    image: tailscale/tailscale:stable
    volumes:
      - /var/lib:/var/lib
      - /dev/net/tun:/dev/net/tun
    environment:
      - TS_AUTH_KEY=${KEY}
    network_mode: "host"
    privileged: true
